# Footprint Library Structure

Footprint library containers are defined as `.pretty` directories.

- `Connectors.pretty/`
- `Power.pretty/`
- `Passives.pretty/`
- `Ethernet.pretty/`

No guessed footprints are included in this starter structure.
Create footprints only after confirming package dimensions and land pattern guidance from manufacturer datasheets.
